#include<stdio.h>
int main()
{
int num;
printf ("enter the elements");
scanf("%d",&num);
//true if num is perfectly divsible
if(num%2==0)
printf("%d is even num");
else
printf("%d is odd num");
return 0;
}
